[project]
name = "mediapipe-test"
version = "0.1.0"
description = "Hand gesture recognition using MediaPipe"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opencv-python>=4.8.0",
    "mediapipe>=0.10.0",
]
